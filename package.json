{
  "name": "hanvanta",
  "version": "0.0.0",
  "private": true,
  "description": "Hanvanta landing page",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build && node scripts/inline-dist.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.59.1",
    "vite": "^8.0.4"
  }
}
